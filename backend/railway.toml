# Railway: đặt Root Directory = backend (repo monorepo) hoặc trỏ Dockerfile vào thư mục này.
# https://docs.railway.com/guides/dockerfiles

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
