# Dùng khi Service "Root Directory" = backend (docker-compose cũng build từ ./backend).
# Nếu deploy từ root monorepo không đổi Root Directory → xem railway.toml ở root repo.
# https://docs.railway.com/guides/dockerfiles

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
